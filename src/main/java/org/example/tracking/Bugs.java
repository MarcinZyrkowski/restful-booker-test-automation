package org.example.tracking;

public interface Bugs {
  String CHECK_IN_BUG =
      "Check in booking filter should return all bookings greater than or equal than provided date but returns only bookings with strick greater date";
}
