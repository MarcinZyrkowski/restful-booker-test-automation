plugins {
    id 'io.qameta.allure' version '2.11.2'
}
allure {
    version = '2.26.0'
}
tasks.withType(io.qameta.allure.gradle.task.AllureReport).configureEach {
    resultsDirs.from(file("build/allure-results"))
}
